services:
  - type: web
    name: stratego-online
    runtime: node
    plan: free
    buildCommand: npm install && npm run build --workspace @stratego/web
    startCommand: npm exec --workspace @stratego/server tsx src/index.ts
    healthCheckPath: /health
